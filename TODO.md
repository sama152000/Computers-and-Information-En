# TODO: Install Bootstrap and PrimeNG in Angular Project

- [x] Install Bootstrap, PrimeNG, and PrimeIcons via npm: `npm install bootstrap primeng primeicons`
- [x] Update angular.json to include Bootstrap CSS in the styles array
- [x] Update src/app/app.config.ts to import PrimeNG modules (e.g., ButtonModule, CardModule, etc.)
- [x] Run `npm install` to ensure dependencies are installed
- [ ] Test the application with `ng serve` to verify imports work correctly
